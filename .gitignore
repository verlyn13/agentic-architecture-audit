# macOS
.DS_Store

# Editor/OS cruft
*~
.idea/
.vscode/

# Python bytecode (scripts/check_drift.py and its self-test)
__pycache__/
*.py[cod]

# Local, personal agent settings — never committed (shared settings live in .claude/settings.json)
.claude/settings.local.json
.codex/

# Audit/profile outputs are produced inside the *target* project, never here.
# If you run this package against itself, keep generated artifacts out of git:
/profile/
/audit/

# Review inputs are "intentionally not bundled" (MANIFEST.md): accepted items are
# transcribed through an ADR; the reports themselves stay out of git.
/improvement-report-*.md
